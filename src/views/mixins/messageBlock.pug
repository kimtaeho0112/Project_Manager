mixin messageBlock(message = {})  
    .projectGoalBlock   
        if !message.isAccepted
            h6=message.title
            h6=message.name
            a(href=routes.profileAnother(message.requestedId)) 
                button 프로필 살펴보기
        else
            h4.projectGoalBlock__charge=editGoal.chargername[0]